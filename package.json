{
	"name": "pm-temuco",
	"private": true,
	"scripts": {
		"build": "turbo run build",
		"clean": "turbo run clean",
		"dev": "turbo run dev && turbo run packages:dev",
		"format": "turbo run format",
		"typecheck": "turbo run typecheck"
	},
	"devDependencies": {
		"tsup": "^8.0.2",
		"tsx": "^4.19.2",
		"turbo": "^2.2.3",
		"typescript": "^5.3.3"
	},
	"packageManager": "pnpm@8.15.6",
	"engines": {
		"node": ">=18"
	},
	"dependencies": {
		"@prisma/client": "^5.10.2",
		"@socket.io/pm2": "^5.3.0",
		"dotenv-cli": "^7.4.2"
	}
}
